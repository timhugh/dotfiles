#!/usr/bin/env bash

set -euo pipefail

sudo pacman -S --noconfirm --needed \
    noto-fonts-emoji

source ../../_util.sh

mkdir -p ~/.local/share/fonts
cp -r "${dot_root}"/fonts/* ~/.local/share/fonts/
fc-cache ~/.local/share/fonts
