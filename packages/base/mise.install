#!/usr/bin/env bash

if ! command -v mise &> /dev/null; then
  echo "Installing mise..."
  curl https://mise.run | sh
fi

mise completion zsh > "${HOME}/.zsh_completions.d/_mise"
chmod +x "${HOME}/.zsh_completions.d/_mise"

mise install
