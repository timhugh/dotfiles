#!/usr/bin/env bash
set -euo pipefail

if [[ $(uname) == "Darwin" ]]; then
  swift ./register_fonts.swift ./fonts
elif [[ $(uname) == "Linux" ]]; then
  mkdir -p ~/.local/share/fonts
  cp -r ./fonts/* ~/.local/share/fonts/
  fc-cache ~/.local/share/fonts
fi
