---
name: c++
needs: dev tools
brew:
  brews:
    - cmake
    - gcc
    - llvm
    - make
  casks:
    - clion
---
name: vcpkg
needs: git
run: mkdir -p "${HOME}/share" && git clone https://github.com/microsoft/vcpkg.git "${HOME}/share/vcpkg"

