# TODO: some things I'd like to figure out and add to this:
# - clicking on the background to bring up expose or whatever its called
# - dark mode
# - show battery percentage in menu bar
# - set clock to 24hr time
# - set display scale to "more space"
# - turn off true tone
---
name: dock config
needs_after: restart dock
run:
  - defaults write com.apple.dock autohide -bool "true"
  - defaults write com.apple.dock show-recents -bool "false"
  - defaults write com.apple.dock minimize-to-application -bool "true"
---
name: mission control config
needs_after: restart system ui server
run:
  - defaults write com.apple.dock mru-spaces -bool "false"
---
name: screenshot tool config
needs_after: restart system ui server
run:
  - defaults write com.apple.screencapture location -string "~/Downloads"
  - defaults write com.apple.screencapture show-thumbnail -bool "false"
---
name: restart dock
run: killall Dock || true
---
name: restart system ui server
run: killall SystemUIServer || true

